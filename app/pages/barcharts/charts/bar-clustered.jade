chart(data="data.filtered", tooltips)
   layout(bar-clustered)
      y(axis, property="State", type="ordinal", show-label, label="State")
      x(axis, tick-format=".2s", grid)
      color(scale="category20", legend)

